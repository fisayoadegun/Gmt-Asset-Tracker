@model Gmt_Asset_Tracker.Models.Physical_check

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Physical_check</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Check_name" class="control-label"></label>
                <input asp-for="Check_name" class="form-control" />
                <span asp-validation-for="Check_name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
